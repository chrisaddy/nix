{
  config,
  pkgs,
  username,
  inputs,
  ...
}: {
  # home.file = {
  #   "${config.home.homeDirectory}/notes" = {
  #     target = "${config.home.homeDirectory}/notes";
  #     recursive = true;
  #   };
  # };
}
